import SwiftUI

struct AuthCloudLogoView: View {
    var body: some View {
        Image("login-logo")
            .resizable()
            .scaledToFit()
            .frame(width: 100, height: 100)
    }
}
