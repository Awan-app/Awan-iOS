import Common
import SwiftUI

struct CalendarView: View {
    var body: some View {
        AppColors.screenBackground.ignoresSafeArea()
    }
}

struct RewardsView: View {
    var body: some View {
        AppColors.screenBackground.ignoresSafeArea()
    }
}

struct YouView: View {
    var body: some View {
        AppColors.screenBackground.ignoresSafeArea()
    }
}
